
language: python

sudo: false

python:
  - 3.5
  - 3.6

env:
  global:
    - PIPENV_VENV_IN_PROJECT=1
    - PIPENV_IGNORE_VIRTUALENVS=1
    - AIRFLOW_HOME=./airflow.cfg

before_install:
  - pip install pipenv
  - pipenv install
install:
  - pipenv install
script:
  - pipenv run pytest tests
